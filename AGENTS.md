# 项目规约

## GPU 测试必须后台跑，父 agent 继续干活

**起了 GPU 测试之后，不要原地等它。** 测试必须放到后台（后台作业或 `nohup ... &`），
父 agent 立刻继续做别的有用的事——写文档、读代码、分析已有轨迹、跑不需要 GPU 的
计算。**禁止**用一串 `sleep` 循环干等测试跑完（那是把每 10 秒一次的轮询当成工作）。

具体做法：

- 启动：用 harness 的后台作业（有完成通知），或 `nohup cmd > log 2>&1 &`。
- 等结果：只在**需要它的时刻**去读一次结果文件，不要为了"盯着"而反复轮询。
- **结果只看 summary 文件**：`grep` 结果行（`Total time` / `sha256` / `SUMMARY`），
  **绝对不要 `tail`/`cat` 测试的原始日志**——里面是 `Load 0 ... Load 100 ...` 这类
  进度条，会把上下文刷爆（犯过：一次 `tail` 刷出几千行进度条，真正要的一行被冲掉）。
- 若确实需要等待，把等待时间用于同一批里的其他独立任务，而不是空转。
- 多个独立测试可以**并行起**（GPU 允许时），不要一个跑完再起下一个。

理由（一次真实代价）：一个 40 秒的基准，我用十个 `sleep 10` 的轮询命令等它，
十次工具调用全花在"还没好"上；与此同时文档没写、代码没读。等价的时间本可以
产出实际结果。GPU 测试的墙钟时间是给机器用的，不是给 agent 用来空等的。

## reboot 之前先跑 `tools/safe_reboot.sh`

**这台机器上，GPU 测试进程还活着的时候发 reboot，关机流程会卡住。** 实测（2026-09-17）：
`systemd-shutdown` 对卡在 NVIDIA 驱动里的进程发 `SIGKILL` 无效（D 状态不可中断），
于是它反复"等进程"，直到硬件 watchdog 到点才复位。默认 `RebootWatchdogSec=10min`，
所以表现是"卡住 10 分钟"，不是死机。

**规约**：reboot 之前先跑

```sh
tools/safe_reboot.sh              # 只报告：目标进程 / GPU 占用 / D 状态进程 / watchdog 值
tools/safe_reboot.sh --clean      # 停掉这类进程并验证，不重启
tools/safe_reboot.sh --reboot     # 清理 + 重启；仍有杀不掉的进程时走紧急路径
```

默认**不动手**。它会列出还可能挡住关机的进程，并在杀不掉时明确告诉你"这次 reboot 会等
watchdog"。想把最坏等待也压掉，用 `--watchdog-sec 120` 把 `RebootWatchdogSec` 降下来。

根因与验证方式见 `docs/gpu_box_reboot_hang.md`。

## 每轮跑完必须自验，别把废数据当样本

**这台机器会偶发四卡掉线，而崩掉的那一轮不只是"没跑完"——它会留下一个
看似正常但内容不完整的日志。** 直接把它当一次样本用，结论就被污染了。

**判据（三条都要过，缺一条即作废）**：

| # | 判据 | 怎么看 |
|---|---|---|
| C1 | 跑完了 | 日志里有 `Total time` |
| C2 | 数值对 | `Token stream sha256` 与已知参照一致（对不上就是数值被污染） |
| C3 | **新代码真的执行了** | 开了开关的那次必须打印只属于新路径的计数（例如 `pcie_ipc AR: <n> collective(s) served`，且 `n > 0`） |

**C3 是最容易被漏掉的一条**，代价也最大：没有它，"开了开关"和"没开"可能**构造上
就是同一个路径**，两次读数一样不是因为没收益，而是因为新代码压根没跑。本会话犯过两次：
- 我自己的 `pcie_ipc`：第一次只打了 `armed`（初始化跑过），**没有发射证据**，我就当"跑了"；
- 另一个 agent 的 `push4`：日志里 `engaged` 是 0 行（新代码没进），却被读成"跑崩了"。

**两次的错误形状完全一样：把"没有输出"读成了"输出是个坏结果"。**

**规约**：任何"改了代码看收益"的测量，报告里必须同时给出 C3 的证据（那个计数、那一行
日志）。给不出 C3 的对照**只能写成"未测出差异，且新路径未被证明执行"**，
不许写成"无收益"。

现成实现：`tools/pcie_ipc_ab.sh`（每轮自验、掉卡即停、不通过的轮次标 VOID）。

## 起 GPU 测试前先干跑自检

**后台启动一个脚本之后，必须立刻确认它"真的到了跑测试那一步"，然后再离开去干别的。**

理由（一次真实代价，2026-09-17）：我写了个 A/B 脚本，没自检就 `nohup` 扔后台。
它第 36 行有个 bash 陷阱——`local a="$1" b="$2" c="${b}..."` 写在**同一行**，
而同一行里的变量展开发生在赋值**之前**，配上 `set -u` 直接判死。**脚本在到达
"包看门狗"那一步之前就退出了**，显卡一次都没动，而我在几轮里以为"测试正在跑"。

**规约（三步，缺一步不算起了）：**

1. **语法检查**：`bash -n <脚本>`。
2. **干跑**：脚本必须有 `DRYRUN=1` 模式——**不碰显卡**，只把它"将要执行的命令行"
   打出来（开关、环境变量、日志路径都要可见）。看一眼输出对不对，再真跑。
3. **启动后立刻验证进程树**：`pgrep -af gpu_watchdog.sh` 要能看到自己那个测试，
   且 `<日志>.gpuwatch.txt` 开始出现内容。看不到就说明**没包住**，立刻停。

第 3 步是把"包了看门狗"从**声称**变成**可核对的事实**——否则"我包了看门狗"这句话
和"脚本压根没跑起来"看起来一模一样。

## GPU 测试必须用看门狗包住

**任何会在 GPU 上跑的东西，都必须用 `tools/gpu_watchdog.sh` 包一层。** 不只是多卡
基准脚本，还包括：单卡测试、nvcc 编出来的自测二进制、合成探针 `.cu`/`.py`、
`nccl_bench` 这类外部小工具、以及任何"我自己随手编一个再跑跑看"的程序。

用法（`<命令...>` 里放真正要跑的东西）：

```sh
tools/gpu_watchdog.sh /tmp/<名字>.out --timeout 300 --label <短名> -- <命令...>
```

它会做四件事，缺一件都不算包住：**判定卡死就立刻抓现场并杀掉**（不等满超时）、
**跑前跑后比对 Xid 计数**、**收工确认显存归零**、把逐卡 util/显存时间线和目标进程
每个线程的 `wchan`/`syscall` 写进 `<日志>.gpuwatch.txt`。

**告警分级（判出来要停手，不要继续往下跑）：**

| 级别 | 判据 | 动作 |
|---|---|---|
| 黄 | 收工时显存没归零 | 先查残留进程，清干净再跑下一个 |
| 橙 | 有新增 Xid，但设备还在、后续能跑 | 停下，先看 Xid 类型；**不要紧接着跑下一个压测** |
| 红 | `Xid 79` / `Xid 154`（GPU has fallen off the bus / 需要重置）或 `nvidia-smi` 报 `Unknown Error` | **立刻停手**，不要重启，也不要反复 `nvidia-smi`；报告给人 |

**红线：绝对不要对已经报 `Xid 79/154` 或 `nvidia-smi` 拿不到句柄的卡做这些动作**
（我犯过，把可恢复的整卡故障做成了全线掉卡）：`kill -9` 持有该卡的进程、
`sleep` 等待后继续重试、`nvidia-smi -r`、往 `/sys/bus/pci/devices/*/remove` 或
`/sys/bus/pci/rescan` 写值。这些动作会让内核在无响应的设备上永久等待，
把"一张卡要重置"升级成"整机 PCIe 链路不可达"。**唯一正确动作是停下来报告。**

理由（一次真实代价，2026-09-17 09:20）：一台四卡 V100 上，我编的 `.cu` 探针直接跑、
没包看门狗；发现后台有一个挂在 `do_exit` 的 benchmark 进程占着四卡各 11 GB，我就
`kill -9` 了它，随后四张卡全部 `Unknown Error`、`Xid 79`（fallen off the bus）+
`Xid 154`（GPU Reset Required），`pcieport` 报 `AER: device recovery failed`。
**根因时间线显示崩溃发生在 kill 之前**，但按红线走的话，我根本不该在那个状态下动它。

**其它老规则的下方保留**（多卡脚本的监控细节），但上面这条覆盖范围更大，
冲突时以上面为准。

## 测试脚本必须监控 GPU 异常

任何跑多卡（TP>1）的测试/基准脚本，都必须同时监控 GPU 异常，不能只等进程退出。至少覆盖：

1. **卡死（deadlock）**——某一卡 util=0 而其他卡持续 >50%，连续若干次采样即判定挂住，**立刻**抓现场并杀掉，不要等满超时。TP 集合通信挂住时，卡住的 rank 没有 kernel（0%），其余 rank 在 NCCL 里空转（100%）；卡住的是哪个 rank 每次可能不同。
2. **Xid / ECC 异常**——跑前 `dmesg | grep -c Xid` 取快照，跑后比对；`nvidia-smi -q` 看 Retired Pages / Pending Page Blacklist 是否变化。
3. **收工是否干净**——进程退出后 GPU 显存必须归零；没归零要报出来。

抓现场要包括：逐卡 util/显存、目标进程每个线程的 `/proc/<tid>/wchan` 与 `/syscall`（用来区分"卡在 CUDA 调用"还是"卡在主机自旋"）、日志最后若干行。

现成实现：`tools/gpu_watchdog.sh`，用法见文件头。**新写测试脚本时直接用它包一层，不要每次现写。**

理由（一次真实代价）：R1 分块流水线挂住时，进程既不报错也不退出，`timeout 900` 白等了十几分钟；只有逐卡 util 才能一眼看出"GPU1 0% / 其余 100%"这个特征。

## 跨对象外推：先证明口径可比，再谈数字

**别把 A 对象的测量结果当成 B 对象的结论。** 报"引擎的 X 是 N"之前，必须能指出 N 是在引擎里量到的；如果 N 来自另一套工具、另一种卡数、另一个负载，那就必须写成"来自 X，**不能外推**"，并且给出为什么口径可比（或不可比）。

失败过的一次（2026-09-17）：我用仓库自带的**两卡**独立 NCCL 工具（`tools/nccl_latency_bench`）量到 10.70 GB/s，就拿它当成了 **FastLLM 四卡 TP4 引擎**的 all-reduce 带宽，进而宣布"引擎的 AR 已跑到链路的 81%"。**那个 10.70 与引擎毫无关系。** 两卡与四卡、独立进程与引擎、空载与满载，三者都不同。

配套的三条硬规则：

1. **指标的分母/分子必须同源。** 用实测的耗时（观测）去除以手推的字节数（假设），得到的"带宽"不是测量值，是**估算**。要么分子分母都来自仪器（例如 NCCL 自己报的 busbw），要么就标成估算并写清假设。
2. **切段（判断某段是 prefill 还是 decode）必须拿证据，不能靠时间轴直觉。** 我在同一个问题上切错了三次（把 prefill 尾巴当 decode、把 decode 当 prefill、把两者混在一起平均）。切段的判据要用段内的**结构特征**（例如集合通信的尺寸分布、专有内核的出现），并印出来核对。
3. **对一个负载求均值之前，先看它的分布。** 6656 次 all-reduce 的最小 0.016 ms、最大 7.7 ms，混在一起求出的"平均每次 5.42 ms"不代表任何一种真实调用。分布是多峰时必须分组报。

## 撤错要指名道姓

写错过的数，撤掉时要写清 **原数是什么、错在哪一步、来自哪次操作**，不能悄悄改掉，也不能只在新版本里修正。本文档上方那条 GPU 监控规则就是一次代价换来的；本节这条是另一次。

## Skills

### Available skills

- git-commit-zh-split: 当用户要求提交代码、整理提交、准备 commit、拆分 commit、push，或指定提交与推送规范时使用。默认使用中文提交信息，将差异较大的改动拆分为多个提交；推送前先执行 fetch、stash、rebase、stash pop，再 push。 (file: /home/tf/codexworkspace/fastllm3/fastllm/.codex/skills/git-commit-zh-split/SKILL.md)
- fastllm-triton-ops: 当需要在 FastLLM CUDA 后端添加、扩展、调试、验证或测速 Triton 生成的算子时使用，覆盖 Python 编译服务、cubin 缓存、C++ fallback 接入和验证流程。 (file: /root/github/newgit2/fastllm/.codex/skills/fastllm-triton-ops/SKILL.md)

### How to use skills

- Discovery: 上面列出的 skill 是当前仓库可用的项目级 skill。
- Trigger rules: 当用户直接提到 skill 名称，或请求明显匹配 skill 描述时，必须使用对应 skill。
- Missing/blocked: 如果 skill 文件缺失或无法读取，简短说明后继续采用最佳替代方案。
- Context hygiene: 先读 `SKILL.md`，只在需要时再加载更多上下文。
